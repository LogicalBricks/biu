.row.page-header
  .span8
    %h1 Lugares recomendados de Oaxaca
  .span4
    = render 'options_buttons'

= render 'normal_lugares' if params[:view].blank?
= render 'list_lugares' if params[:view] == 'list'
= render 'thumbnail_lugares' if params[:view] == 'thumbnail'
= render 'mapa_lugares' if params[:view] == 'mapa'
